version: '3.8'

services:
  promptshield:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./plugins:/app/plugins
      - ./data:/app/data
